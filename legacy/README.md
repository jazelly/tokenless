# Legacy Tokenless path

This directory archives the former browser-extension and Chrome Native Messaging host path.

The active Tokenless product is the managed Playwright workflow:

`Agent → CLI or local API → local daemon → Playwright worker → managed browser profile → provider website`

Nothing under `legacy/` is part of the default workspace build, published npm packages, or user-facing setup. Keep it for historical reference and optional local experiments only.
