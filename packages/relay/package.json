{
  "name": "tokenless-relay",
  "version": "0.1.0",
  "description": "Tokenless Relay HTTP entrypoint for web and hosted integrations.",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "bin": {
    "tokenless-relay": "dist/src/server.mjs"
  },
  "files": [
    "dist/src",
    "package.json"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "engines": {
    "node": ">=22"
  }
}
