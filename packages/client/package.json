{
  "name": "tokenless-client",
  "version": "0.1.0",
  "description": "Webapp client helpers for Tokenless Relay and extension integrations.",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "exports": {
    ".": "./dist/src/index.js"
  },
  "files": [
    "dist/src",
    "package.json"
  ],
  "scripts": {
    "build": "node -e \"require('node:fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "prepack": "npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
