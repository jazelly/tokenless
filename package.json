{
  "name": "tokenless",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Standalone visible-session bridge and isolated direct AI provider CLI.",
  "scripts": {
    "build": "npm run build --workspaces --if-present && npm run build:scripts",
    "build:scripts": "node -e \"require('node:fs').rmSync('dist/scripts',{recursive:true,force:true})\" && tsc -p tsconfig.scripts.json",
    "changeset": "changeset",
    "lint": "npm run lint --workspaces --if-present && tsc -p tsconfig.scripts.json --noEmit",
    "release:version": "node scripts/release/version.mjs",
    "verify:extension-release": "node scripts/verify-extension-release.mjs",
    "sync:skill": "npm run build:scripts && node dist/scripts/sync-tokenless-skill.mjs",
    "test": "npm run build && node --test --test-concurrency=1 test/**/*.test.mjs",
    "test:e2e": "npm run build && TOKENLESS_E2E=1 node --test test/**/*.e2e.mjs",
    "test:e2e:live-direct": "npm run build:js --workspace tokenless && TOKENLESS_LIVE_DIRECT=1 node --test test/live-direct-api.e2e.mjs",
    "test:e2e:live-chatgpt": "npm run build && TOKENLESS_LIVE_CHATGPT=1 node --test test/live-chatgpt.e2e.mjs"
  },
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=22"
  },
  "packageManager": "npm@10",
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@playwright/test": "^1.53.0",
    "@types/chrome": "^0.2.2",
    "@types/node": "^26.1.1",
    "selfsigned": "^5.5.0",
    "typescript": "^7.0.2"
  }
}
