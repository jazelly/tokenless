---
"tokenless": minor
---

Replace managed browser profile imports with headed native connections to the user's running Google Chrome 144+ instance. Tokenless now checks support by connecting, manages only its own tabs, and disconnects without closing Chrome when the daemon stops.
