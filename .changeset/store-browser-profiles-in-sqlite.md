---
"tokenless": patch
---

Store the Tokenless API browser profile registry in `tokenless.sqlite3` and import the existing `browser/profiles.json` registry once.

将 Tokenless API browser profile registry 存入 `tokenless.sqlite3`，并一次性导入现有的 `browser/profiles.json` registry。
