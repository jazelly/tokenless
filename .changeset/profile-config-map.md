---
"tokenless": minor
---

Store complete per-profile provider configuration in `config.json` under `profiles`, migrate the legacy side table with the registered profiles, and expose profile configuration directly in CLI and dashboard payloads.
