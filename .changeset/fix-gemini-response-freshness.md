---
"tokenless": patch
---

Fix reused Gemini conversations so each response read returns the current turn instead of a previous reply.

修复复用 Gemini conversation 时的 response 读取，确保每次返回当前 turn，而不是之前的回复。
