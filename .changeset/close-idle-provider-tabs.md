---
"tokenless": patch
---

Close every released Tokenless API-owned provider tab after its idle timeout and let the Dashboard reopen an empty profile browser on demand.

在空闲超时后关闭所有已释放且由 Tokenless API 拥有的 provider tabs，并允许 Dashboard 按需重新打开没有 tab 的 profile 浏览器。
