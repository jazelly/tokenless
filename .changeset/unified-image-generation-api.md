---
"tokenless": minor
---

Add one authenticated `/v1/images/generations` endpoint for browser and direct image generation, route CLI image runs through it, and remove the public G4F-namespaced image route.
