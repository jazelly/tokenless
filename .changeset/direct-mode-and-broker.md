---
"tokenless": minor
---

Add opt-in direct execution for ChatGPT, Claude, Gemini, Grok, and Antigravity, plus an authenticated loopback streaming API broker, while preserving visible extension mode as the default.
