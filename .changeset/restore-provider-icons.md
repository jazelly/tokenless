---
"tokenless": patch
---

Restore the missing Lovable and Agnes provider icons (`assets/providers/lovable.svg`, `assets/providers/agnes.svg`) so both providers render correctly in the dashboard capability matrix and README provider tables.
