---
"tokenless": minor
---

Add a dedicated setup route that discovers installed Chrome, Brave, and CloakBrowser runtimes, lets users add a verified native executable path, and saves the selected runtime with the config-wired default profile.

新增专用 setup 路由，发现已安装的 Chrome、Brave 和 CloakBrowser runtime，允许用户添加经过验证的原生 executable path，并将所选 runtime 与配置关联的默认 profile 一起保存。
