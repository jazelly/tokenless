---
"tokenless": patch
---

Honor the OpenAI Responses `store` flag for the local response ledger. Default storage remains enabled; `store: false` disables ledger continuation for that response while ordinary job history and provider-side retention remain separate.
