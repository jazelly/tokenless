---
tokenless: minor
---

Add explicit `tokenless setup --install-codex` onboarding and bind Codex CLI calls to concrete `CODEX_THREAD_ID` tasks while preserving Hook session-tree provenance and automatically reusing native provider workspaces.
