---
"tokenless": patch
---

Bundle matching Tokenless API skills with npm and macOS releases, synchronize complete skill directories during install/setup/upgrade, and add a skill-only sync command with full doctor verification. Update the installation skill and shell installer to the current setup workflow.

随 npm 与 macOS 发布包分发配套 Tokenless API skills，在 install/setup/upgrade 中同步完整目录，并增加独立同步命令与 doctor 完整性检查。更新安装 skill 和 Shell 安装器以使用当前 setup 流程。
