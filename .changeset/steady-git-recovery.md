---
"tokenless": patch
---

Guide Terminal-Bench Git recovery through existing Git metadata and make benchmark Harness delegation distinguish workspace content searches from shell commands.

引导 Terminal-Bench Git recovery 使用现有 Git metadata，并让 benchmark Harness delegation 明确区分 workspace content search 与 shell command。
