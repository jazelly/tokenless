---
'tokenless': minor
---

Let DeepSeek Harness benchmark agents inspect one file, delegate one bounded read-only workspace pass, then batch or locally search the permitted changes and verify them. Record safe live-page limit evidence, configured provider plan observations, and per-interaction token estimates in Terminal-Bench reports.
