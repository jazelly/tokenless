---
"tokenless": patch
---

Require the Terminal-Bench Git recovery parent to execute recovery through its bash tool before returning a final response.

要求 Terminal-Bench Git recovery parent 在返回最终响应前通过 bash tool 执行恢复操作。
