---
"tokenless": patch
---

Let production managed browsers use native credential storage so compatible copied profiles can preserve macOS Keychain-encrypted sign-in state, and document the expected Safe Storage prompt.
