---
"tokenless": minor
---

Add the durable `tokenless agent run|read|resume|cancel` workflow with authenticated daemon routing, local stdio MCP catalogs, exact approval arguments, and restart-safe provider continuation.
