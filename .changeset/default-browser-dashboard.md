---
"tokenless": patch
---

Open the Tokenless Dashboard in the operating system's default browser instead of the selected provider profile browser.

使用操作系统默认浏览器打开 Tokenless Dashboard，不再使用所选 provider profile 的浏览器。
