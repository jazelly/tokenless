---
"tokenless": patch
---

Classify uncertain visible prompt-submission failures as unknown-window provider rate limits so automatic routing can fall back before any provider submission occurs.

将不确定的可见 prompt 提交失败归类为未知窗口的 provider rate limit，使自动路由可以在 provider 尚未收到提交时安全 fallback。
