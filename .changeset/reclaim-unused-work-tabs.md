---
"tokenless": patch
---

Reclaim unused Tokenless API work tabs after the idle timeout, including provider homepages and tabs left by failed, canceled, or input-only jobs. Active tasks and user pages remain protected, user interaction resets the idle timer, and one blank tab keeps the browser running.
