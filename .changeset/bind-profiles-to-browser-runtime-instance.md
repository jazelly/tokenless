---
"tokenless": patch
---

Bind newly created and setup-managed profiles to the exact browser executable that created them, while retaining the existing protection against opening a profile with an older browser version.

将新建和通过 setup 管理的 managed profile 绑定到创建它的确切 browser executable，同时保留现有的旧版本浏览器保护。
