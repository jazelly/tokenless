---
"tokenless": patch
---

Accept valid strict JSON tool-protocol finals whose text contains Markdown code fences, while continuing to reject multiple outer response fences.

接受 final text 含 Markdown code fence 的有效 strict JSON tool-protocol response，同时继续拒绝多个 outer response fences。
