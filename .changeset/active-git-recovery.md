---
"tokenless": patch
---

Make the Terminal-Bench Git recovery parent perform and verify the recovered change instead of returning instructions.

让 Terminal-Bench Git recovery parent 实际执行并验证恢复操作，而不是只返回操作说明。
