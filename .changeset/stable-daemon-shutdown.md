---
"tokenless": patch
---

Stop the daemon only after background output-savings work has released the shared database, and let live release gates inspect an already-resident configured browser.

等待后台 output-savings 工作释放共享数据库后再停止 daemon，并允许实时发布门禁检查已常驻的配置浏览器。
