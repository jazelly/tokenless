---
"tokenless": patch
---

Read completed Claude code-block responses and admit its real-provider-proven single-call strict tool and tool-history scope to `tokenless/auto` routing.

读取已完成的 Claude code-block response，并将真实 provider 已验证的 single-call strict tool 与 tool-history scope 纳入 `tokenless/auto` routing。
