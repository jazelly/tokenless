---
"tokenless": minor
---

Separate provider-native workflows from canonical capabilities, remove `skill.invoke`, and deliver caller-selected Harness Skills through the standard chat and file-upload route.

将 provider-native workflow 与 canonical capability 分离，删除 `skill.invoke`，并通过标准 chat 与 file-upload route 交付 caller-selected Harness Skills。
