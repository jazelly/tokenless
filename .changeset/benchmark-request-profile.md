---
"tokenless": patch
---

Honor the selected managed profile in API requests so Terminal-Bench parent and child runs use the same provider router.

Allow the existing bounded tool-response correction to recognize protocol headers formatted with JSON whitespace.
