---
"tokenless": patch
---

Wait for Qwen attachments to finish visible parsing before prompt submission, use provider-specific interaction windows, and require Playwright actionability before a single real submit click.

在提交 prompt 前等待 Qwen 附件完成可见解析，使用 provider-specific 交互等待窗口，并在唯一一次真实提交点击前通过 Playwright actionability 检查。
