---
"tokenless": patch
---

Add `tokenless upgrade --json` to update the global CLI, refresh Tokenless agent skills, reconcile the packaged local daemon through the newly installed CLI, and report the final read-only doctor result.
