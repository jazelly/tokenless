---
"tokenless": patch
---

Add prompt-free `tokenless upgrade` with concise human progress and a `--json` automation mode to update the global CLI, refresh Tokenless agent skills, reconcile the packaged local daemon through the newly installed CLI, and report the final read-only doctor result. Document it in the installation skill as the canonical maintenance path, including one-time bootstrap and partial-failure recovery guidance.
