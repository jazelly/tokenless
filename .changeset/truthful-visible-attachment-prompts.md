---
"tokenless": patch
---

Describe separately supplied visible attachments truthfully in compiled prompts instead of claiming that no relevant files were attached, and accept `--attach-file` in the local prompt preview.

已单独提供可见附件时，在编译后的 prompt 中如实说明，不再声称未附加相关文件；同时允许本地 prompt 预览接收 `--attach-file`。
