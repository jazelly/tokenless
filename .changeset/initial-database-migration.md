---
"tokenless": minor
---

Ship the initial SQLite schema migration with Tokenless API and Tokenless Harness. Upgrade the current unversioned database without replacing existing tables or data, record schema versions, and fail atomically on unsupported schemas.

随 Tokenless API 和 Tokenless Harness 发布初始 SQLite schema migration。在不替换已有表或数据的情况下接管当前无版本数据库，记录 schema 版本，并在遇到不支持的结构时原子失败。
