---
"tokenless": patch
---

Align the Dashboard header metrics on one line, report idle runtime activity accurately, and add a finished-job count.

将 Dashboard header 指标对齐为单行，准确显示空闲 runtime 状态，并增加已结束任务计数。
