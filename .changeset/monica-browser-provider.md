---
"tokenless": minor
---

Add an experimental Monica browser provider. `tokenless/monica` submits prompts on monica.im through the visible composer (Enter-key submission) and reads assistant answers from the rendered markdown surface.