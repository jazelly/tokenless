interface:
  display_name: "Tokenless API Install / 安装"
  short_description: "Install and verify Tokenless API / 安装与验证"
  default_prompt: "Use $tokenless-install to inspect my platform and installation channel, perform the requested Tokenless API maintenance, and verify the result. / 使用 $tokenless-install 检查平台与安装渠道，执行请求的 Tokenless API 维护并验证结果。"
