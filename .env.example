# Copy this file to .env and point it at a complete dedicated Tokenless test config.
# The harness reads its default and additional profiles from the adjacent production registry.
TOKENLESS_TEST_CONFIG=/absolute/path/to/tokenless-test-home/config.json
