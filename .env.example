# Copy this file to .env and point it at a complete Tokenless home.
# The adjacent registry's default profile is the one persistent profile used by every browser test.
TOKENLESS_TEST_HOME=/absolute/path/to/tokenless-home
# Required only for the Jev (TypeSafe) router engine and its tests.
TYPESAFE_API_KEY=
